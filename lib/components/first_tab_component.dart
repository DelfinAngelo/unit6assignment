
import 'package:flutter/material.dart';

class FirstTabComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'This is the Profile tab',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
