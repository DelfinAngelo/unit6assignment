
import 'package:flutter/material.dart';

class SecondTabComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'This is the Details tab',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
